# Secret used to sign/verify the auth session cookie (HMAC-SHA256).
# Generate a real one for local dev: `openssl rand -base64 32`
# Must also be set as a Vercel project environment variable in production/preview.
SESSION_SECRET=
